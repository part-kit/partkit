{
  "name": "partkit-monorepo",
  "private": true,
  "type": "module",
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "scripts": {
    "build": "tsc -b",
    "test": "vitest run",
    "check": "tsc -b && vitest run",
    "registry:publish": "node scripts/publish-part.mjs",
    "registry:check": "node scripts/publish-part.mjs --check"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "better-auth": "^1.6.16",
    "typescript": "^5.6.0",
    "vitest": "^3.0.0"
  },
  "engines": {
    "node": ">=22"
  }
}
